import SectionInfo from "../../../components/SectionInfo";

export default function ToolsInfo() {
  return (
    <SectionInfo
      eyebrow="Instruments"
      title="Search the tech stack"
      description="A fun way to search through the tools I use to build projects"
    />
  );
}
