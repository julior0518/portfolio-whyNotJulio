import SectionInfo from "../../../components/SectionInfo";

export default function ToolsInfo() {
  return (
    <SectionInfo
      eyebrow="Instrumentarium"
      title="Tools & craft"
      description="A calm inventory — search the stack you need; the rest stays in the wings."
    />
  );
}
