/**
 * @deprecated Import `AlpineParallaxStage` from `./hero/AlpineParallaxStage` for new work.
 * Kept as a thin alias so any stale imports keep working.
 */
export { default } from "./hero/AlpineParallaxStage";
